/**
 * ? variable declear - var(we will not be using this) , let(changing allowed), const(changing not allowed)
 * ? premitive data types --> numbers , boolean, string, symbol, null, undefined, bigInt
 * ? non-premetive data types --> array , object
 * ? conditionals ---> if-else, or ternary(?)
 * ? conditional operators ---> >, <, >= , <=, ==, ===
 * ? logical operator ---> &&, ||, !
 * ? loops ---> for, for-of, while, do-while
 * ? for objects looping ---> for-in
 * ? break and continue in loop
 */

const numbers = [1, 2, 3, 4, 5, 67]; //? index starts from0
