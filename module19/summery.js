/**
 * ? function is used to make the code reuseable
 * ? a set of statement that performs it's task when it is called
 * ? maintains a relationship with the input and the output
 * ? we can pass arguments to function while the funtion receives with parameter
 * ? from the function we can also return values if needed
 * ? function, parameter, arguments return,
 */
